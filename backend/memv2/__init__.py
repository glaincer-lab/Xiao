"""backend.memv2 —— M2 情感与姿态工程包。

模块规划（与 M1 的 backend.memv1 / M0 的 backend.gateway 并存，互不覆盖）：
- ``posture``   ：四态姿态判定卡规则引擎（M2-A，本子项目）
- ``phrases``   ：话术库 YAML 加载 + 按姿态×情绪×亲密度选变体（M2-D）
- ``shadow``    ：影子日志——真路由上线前只记录判定结果、绝不切换姿态（M2-D）

注意：为避免在本包导入时强制要求其它子模块存在，本 __init__ 不自动 import
任何子模块，各子模块按契约独立引入。
"""
