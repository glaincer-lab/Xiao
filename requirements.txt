# 核心依赖（推荐 Python 3.11 / 3.12；勿用 3.14，torch/FunASR 尚无对应 wheel）
fastapi>=0.110
uvicorn[standard]>=0.29
python-dotenv>=1.0
PyYAML>=6.0
tokenizers>=0.19  # 可选增强：网关语义消歧（semantic_filter），未装则自动降级为规则
numpy>=1.26
sqlite-vec==0.1.9  # 向量主选后端（Win x64 预编译 wheel，MIT+Apache-2.0；装不上自动回退 numpy 暴力兜底，绝不因缺后端无法启动）
sounddevice>=0.4.6
webrtcvad-wheels>=2.0.10
sherpa-onnx>=1.13
onnxruntime>=1.17
openai>=1.30
dashscope>=1.27  # qwen3-asr-flash-realtime 需要含 dashscope.audio.qwen_omni（OmniRealtime）
edge-tts==7.2.8
pygame>=2.5.0
requests>=2.31
beautifulsoup4>=4.12
websockets>=12.0
Pillow>=10.0  # 语音操电脑：截屏看图（screen_look）

# 可选依赖（按需安装，不装不影响其他功能）：
# comtypes          语音操电脑的 UIA 窗口元素树（uia_dump）：pip install comtypes
